exec node DummyImageClassification:
    let D: data = data node *
    let M: model = node *

    let loss = node CrossEntropy
    let optimizer = optim node Adam

    let epoch = int *
    let batch size = int *

    1. data + model + loss + optimizer
