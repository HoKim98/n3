exec node ImageClassification:
    let D: dataset = data node *
    let M: model = node *

    let loss = node CrossEntropy
    let optimizer = node Adam

    let epoch = int *
    let batch size = int *
